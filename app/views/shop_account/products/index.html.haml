%h1 Products
%table.table
  %thead.thead-dark
    %tr
      %th Name
      %th Desc
      %th Images
      %th Price
      %th{:colspan => "3"}
  %tbody
    - @products.each do |product|
      %tr
        %td= product.name
        %td
          = product.desc[0..30]
          \...
        %td= image_tag product.images_url, :class => "image-thumb" if product.images?
        %td= product.price
        %td= link_to 'Show', shop_account_product_path(product), :class => "btn btn-info"
        %td= link_to 'Edit', edit_shop_account_product_path(product), :class => "btn btn-info"
        %td= link_to 'Destroy', shop_account_product_path(product), method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-warning"
= paginate @products
= link_to 'New Product', new_shop_account_product_path, :class => "btn btn-primary mt-2"
