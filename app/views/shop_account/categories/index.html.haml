%h1 Categories
%table.table
  %thead.thead-dark
    %tr
      %th Name
      %th{:colspan => "3"}
  %tbody
    - @categories.each do |category|
      %tr
        %td= link_to category.name, category
        %td= link_to 'Show', shop_account_category_path(category), class: "btn btn-info"
        %td= link_to 'Edit', edit_shop_account_category_path(category), class: "btn btn-info"
        %td= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger"
%br/
= link_to 'New Category', new_shop_account_category_path, class: "btn btn-primary"
