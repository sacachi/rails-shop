.container
  .row
    .col-lg-3
      %h1.my-4 Shop Name
      .list-group
        - @categories.each do |category|
          =link_to(category.name, category_path(category), html_options = { class: "list-group-item"})
    .col-lg-9
      =yield



